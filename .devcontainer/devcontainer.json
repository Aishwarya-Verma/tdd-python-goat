{
  "name": "TDD Python Django Selenium Codespace",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu",
  "postCreateCommand": "apt-get update && apt-get install -y git python3 python3-pip chromium firefox-esr chromium-driver geckodriver && pip3 install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  }
}
